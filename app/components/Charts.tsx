'use client';
import { Area, AreaChart, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis, Bar, BarChart } from 'recharts';import { Card,Badge } from './ui/Card';import type { EnsoStatus, ForecastPoint, ModelTrace } from '../lib/types';
export function TrendChart({status}:{status:EnsoStatus}){return <Card><Badge>Interactive Charts</Badge><h2 className="mt-3 text-2xl font-bold">ONI, Niño 3.4 and SOI timeline</h2><div className="mt-5 h-80"><ResponsiveContainer><LineChart data={status.trend}><CartesianGrid strokeDasharray="3 3" stroke="#ffffff20"/><XAxis dataKey="month" stroke="#9cc"/><YAxis stroke="#9cc"/><Tooltip contentStyle={{background:'#08111f',border:'1px solid #27506d'}}/><Legend/><Line type="monotone" dataKey="nino34" stroke="#fb923c" dot={false} strokeWidth={3}/><Line type="monotone" dataKey="oni" stroke="#38bdf8" dot={false}/><Line type="monotone" dataKey="soi" stroke="#a78bfa" dot={false}/></LineChart></ResponsiveContainer></div><div className="mt-3 flex gap-2"><a className="rounded-full bg-white/10 px-4 py-2" href="/api/export">Export CSV</a><button className="rounded-full bg-white/10 px-4 py-2">Export PNG</button></div></Card>}
export function ForecastCharts({points,models}:{points:ForecastPoint[];models:ModelTrace[]}){return <div className="grid gap-5 lg:grid-cols-2"><Card><Badge>Forecast Model Center</Badge><h2 className="mt-3 text-2xl font-bold">ENSO phase probabilities</h2><div className="mt-5 h-80"><ResponsiveContainer><AreaChart data={points.slice(0,12)}><CartesianGrid strokeDasharray="3 3" stroke="#ffffff20"/><XAxis dataKey="season" stroke="#9cc"/><YAxis tickFormatter={(v)=>`${Math.round(Number(v)*100)}%`} stroke="#9cc"/><Tooltip formatter={(v)=>`${Math.round(Number(v)*100)}%`} contentStyle={{background:'#08111f',border:'1px solid #27506d'}}/><Legend/><Area dataKey="elNino" stackId="1" stroke="#fb923c" fill="#fb923c99"/><Area dataKey="neutral" stackId="1" stroke="#94a3b8" fill="#94a3b899"/><Area dataKey="laNina" stackId="1" stroke="#60a5fa" fill="#60a5fa99"/></AreaChart></ResponsiveContainer></div></Card><Card><Badge>Spaghetti Plot</Badge><h2 className="mt-3 text-2xl font-bold">Model spread and uncertainty bands</h2><div className="mt-5 h-80"><ResponsiveContainer><LineChart><CartesianGrid strokeDasharray="3 3" stroke="#ffffff20"/><XAxis dataKey="season" type="category" allowDuplicatedCategory={false} stroke="#9cc"/><YAxis stroke="#9cc"/><Tooltip contentStyle={{background:'#08111f',border:'1px solid #27506d'}}/><Legend/>{models.map((m,i)=><Line key={m.model} dataKey="value" data={m.values} name={m.model} dot={false} stroke={['#fb923c','#22d3ee','#a78bfa','#facc15','#4ade80','#f472b6'][i]} />)}</LineChart></ResponsiveContainer></div></Card></div>}
export function AnalyticsBars({status}:{status:EnsoStatus}){const data=Object.entries(status.advanced).map(([label,value])=>({label,value}));return <Card><Badge>Expert Mode Analytics</Badge><h2 className="mt-3 text-2xl font-bold">Kelvin wave, heat content, trades, Walker circulation</h2><div className="mt-5 h-72"><ResponsiveContainer><BarChart data={data}><CartesianGrid strokeDasharray="3 3" stroke="#ffffff20"/><XAxis dataKey="label" stroke="#9cc"/><YAxis stroke="#9cc"/><Tooltip contentStyle={{background:'#08111f',border:'1px solid #27506d'}}/><Bar dataKey="value" fill="#38bdf8" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div></Card>}
